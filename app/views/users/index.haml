= link_to 'Add new', new_user_path, class: 'btn btn-default pull-right align-with-h1'
%h1 users
%table.table.table-hover.table-striped
  %thead
  %tr
    %th Name
    %th Name
  - @users.each do |user|
    %tr
      %td= user.name
      %td= link_to 'Delete', user_path(user), method: 'delete'
